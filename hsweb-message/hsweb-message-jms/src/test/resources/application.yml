spring:
    activemq:
        in-memory: true
    datasource:
       url : jdbc:h2:mem:test_mem
       username : sa
       password :
       type: com.alibaba.druid.pool.DruidDataSource
       driver-class-name : org.h2.Driver
    jms:
      pub-sub-domain: true

hsweb:
    app:
      name: jms测试
      version: 3.0.0