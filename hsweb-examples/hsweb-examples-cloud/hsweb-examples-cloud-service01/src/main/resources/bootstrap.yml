spring:
  application:
    name: service01
  cloud:
    discovery:
      client:
        simple:
          local:
            service-id: service01
eureka:
  client:
    service-url:
        defaultZone: http://${eureka-host:localhost}:${eureka-port:8761}/eureka/